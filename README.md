frontend
```
cd frontend
npm install
npm run dev
```

backend
```
cd backend
python3 -m venv venv
source venv/bin/activate  # (or venv\Scripts\activate on Windows)
pip install flask
python3 app.py
```